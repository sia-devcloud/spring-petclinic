trigger: none

pool:
  vmImage: ubuntu-latest

steps:
  - task: Docker@2
    inputs:
      containerRegistry: 'MyDockerHub'
      repository: 'siadevops/springpetclinic'
      command: buildAndPush
      Dockerfile: 'Dockerfile'
      tags: $(Build.BuildId)